spring:
  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
    locations: classpath:db/migration/V1__init_users
    baseline-on-migrate: true

springdoc:
  api-docs:
    enabled: true

  swagger-ui:
    enabled: true