spring:
  application:
    name: doctor-service
  profiles:
    active: ${DOCTOR_SERVER_PROFILES:dev}
  config:
    import: optional:configserver:http://${CONFIG_SERVER_HOSTNAME:localhost}:${CONFIG_SERVER_PORT:8888}

server:
  port: ${DOCTOR_SERVER_PORT}